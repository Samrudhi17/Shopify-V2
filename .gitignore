# .NET
bin/
obj/
*.user

# Node / Vite
node_modules/
frontend/dist/
frontend/.env

# Env / secrets
.env
*.local
appsettings.*.local.json

# Uploaded files (runtime data, not source)
backend/QRShop.API/wwwroot/uploads/

# Firebase
firebase-service-account*.json

# OS / editor
.DS_Store
.vs/
.vscode/
.idea/

# Deploy script artifacts (scripts/deploy.sh)
/backups/
/.deploy-state

# Local editor / tooling config — personal to each machine.
.claude/

# Local-only compose additions (exposes the DB port for GUI clients).
docker-compose.override.yml
